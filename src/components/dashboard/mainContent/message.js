import { Box } from "@mui/material";

const Message = () => {
  return (
    <Box
      style={{
        display: "flex",
        padding: "1em",
        backgroundColor: "purple",
      }}
    >MESSAGES</Box>
  );
};

export default Message;
