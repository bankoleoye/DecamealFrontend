import { Box } from "@mui/material";

const Timetable = () => {
  return (
    <Box
      style={{
        display: "flex",
        padding: "1em",
        backgroundColor: "blue",
      }}
    >TIMETABLE</Box>
  );
};

export default Timetable;
